import 'package:flutter/material.dart';

class OngoingTaskCardWidget extends StatelessWidget {
  const OngoingTaskCardWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 120,
      color: Colors.white,
    );
  }
}
